import React from "react";

const RightSidebar = ({
  imgUrl,
  title,
  description,
  podcastId,
}: {
  imgUrl: string;
  title: string;
  description: string;
  podcastId: string;
}) => {
  return <section className="right_sidebar"></section>;
};

export default RightSidebar;
